# Unreleased

This is a major release where we dropped support for Python 3.9.

## Refactoring

* #283: Updated to `exasol-toolbox` 3.0.0 and dropped support for Python 3.9
* #285: Updated optional dependency `pyarrow` to `^22.0.0` and modified CI to run with Python 3.14
