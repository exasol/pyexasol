# 2.2.3 - 2026-07-15

## Summary

In this patch release, the type hint for `ExaConnection.set_autocommit` was correctly
restored to a `bool`.

## Bugfixes

* #350: Fixed type hint for `ExaConnection.set_autocommit`